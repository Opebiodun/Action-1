name: Generate ASCII Artwork2

on: push

jobs:
    ascii_job:
        runs-on: ubuntu-latest
        steps:
        - name: checkout repo
          uses: actions/checkout@v4 

        - name: List repo files
          run: ls -ltra
          
        - name: Make script executable
          run: chmod +x ascii-script.sh
             
        - name: Run ASCII script
          run: ./ascii-script.sh